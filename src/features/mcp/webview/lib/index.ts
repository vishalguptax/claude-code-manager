/**
 * Barrel for the MCP slice's pure helpers (lib segment). No JSX, no state.
 */
export {
  MCP_BROWSE_URL,
  buildRows,
  connectionPreview,
  groupLabel,
  maskSensitiveValue,
  type Row,
} from "./helpers";
