/**
 * Barrel for the MCP slice's reactive state (model segment).
 */
export {
  type McpScopeFilter,
  applyError,
  applyServers,
  errorMessage,
  filteredServers,
  loading,
  resetMcpSignals,
  scopeCounts,
  scopeFilter,
  searchQuery,
  selected,
  servers,
} from "./signals";
