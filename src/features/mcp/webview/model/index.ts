/**
 * Barrel for the MCP slice's reactive state (model segment).
 */
export {
  type McpScopeFilter,
  applyAuthNeeds,
  applyError,
  applyServers,
  authNeeds,
  errorMessage,
  filteredServers,
  loading,
  resetMcpSignals,
  scopeCounts,
  scopeFilter,
  searchQuery,
  selected,
  servers,
} from "./signals";
