/**
 * Barrel for the commands feature lib segment — pure, JSX-free helpers for
 * row grouping, previews, and clipboard copy.
 */
export { buildRows, copyCommand, groupLabel, previewText, type Row } from "./rows";
