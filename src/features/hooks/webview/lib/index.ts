/**
 * Barrel for the hooks slice's pure helpers (display-label maps, matcher
 * formatting). No JSX, no signals — safe to import from any segment.
 */
export { eventLabel, matcherDisplay, scopeLabel } from "./labels";
