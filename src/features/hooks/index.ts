/**
 * Hooks feature barrel — exports the parser and all hook types.
 */
export { parseHooks } from "./parser";
export type { Hook, HookEvent, HooksExtensionMessage, HooksWebviewMessage } from "./types";
