/**
 * Account webview — F1 stub. F2 will replace this with the ported Preact UI.
 */
import { EmptyState } from "../../../webview/components/EmptyState";

export default function AccountTab() {
  return (
    <EmptyState
      title="Account migration pending"
      description="This tab will be migrated to Preact in F2."
    />
  );
}
