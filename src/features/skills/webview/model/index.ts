/** Barrel for the skills model segment: reactive feature state. */
export {
  claudeCodeInstalled,
  countByScope,
  filteredSkills,
  marketplaceSkillsUrl,
  type ScopeFilter,
  scopeFilter,
  searchQuery,
  selectedSkill,
  skills,
} from "./signals";
