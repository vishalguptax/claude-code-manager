/**
 * Barrel for the config slice's model segment — reactive signals and the
 * test-only reset helper.
 */
export {
  _resetConfigState,
  configData,
  configError,
  loading,
  permissionScope,
  permissionSearch,
} from "./signals";
