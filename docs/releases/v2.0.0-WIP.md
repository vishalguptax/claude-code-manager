# v2.0.0 — Work In Progress

Per-feature changelog entries assembled during the v2 Preact revamp. The
integration session (F3) consolidates these into the final `CHANGELOG.md`.

## sessions

- Migrated the sessions tab to Preact with `@preact/signals` feature state.
- Session list is virtualized (`<VirtualList />`) so 5,000+ sessions scroll in
  constant time.
- Live status dots, search (250 ms debounce), project/branch/date filters, bulk
  mode, and the windowed detail view all rebuilt as Preact components.
- All inbound webview messages are validated against the shared valibot schema
  on the host before dispatch; outbound sends go through typed protocol
  helpers.
- Added `search`, `filter`, `deleteSession`, `copyMarkdown`, `openFile`, and the
  host→webview `sessions.delta` message types to the shared protocol.
- Behavior note: date-group headers inside the list and persisted filter
  selections are dropped for v2 (the virtualizer needs uniform rows and the
  shell's persistence wiring lands later); pinned-first ordering is preserved.
